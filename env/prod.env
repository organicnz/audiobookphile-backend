# =============================================================================
# AUDIOBOOKPHILE — PRODUCTION ENVIRONMENT (single source of truth)
# =============================================================================
# Canonical production values. Secrets are NOT stored here — they live in
# Vercel project env vars / Supabase Vault / Edge Function secrets.
# Use this file as the reference when configuring the platforms, and to
# generate local prod-shaped env files via:
#
#   ./scripts/sync-env.sh --prod

# --- Backend (Supabase) ------------------------------------------------------
# [backend]
SUPABASE_PROJECT_ID=iambzzclljayqdxkeepy
SUPABASE_ACCESS_TOKEN=
CRON_SECRET=
GITHUB_TOKEN=
B2_SECONDARY_KEY_ID=
B2_SECONDARY_APP_KEY=
B2_SECONDARY_ENDPOINT=
B2_SECONDARY_REGION=
B2_SECONDARY_BUCKET_NAME=
ACTIVE_B2_TIER=

# --- Web (Next.js) ----------------------------------------------------------
# [web]
NEXT_PUBLIC_SUPABASE_URL=https://iambzzclljayqdxkeepy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=https://audiobookphile.vercel.app
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
SUPABASE_SERVICE_ROLE_KEY=

# --- Maestro E2E (iOS app) ----------------------------------------------------
# [maestro]
SERVER_URL=https://audiobookphile.vercel.app
TEST_EMAIL=
TEST_PASSWORD=
MAGIC_LINK_URL=
